{
  "title": "InventoryModel",
  "type": "object",
  "properties": {
    "slug": {
      "title": "Slug",
      "description": "Slug uniquely identifying the inventory",
      "type": "string"
    },
    "inventory": {
      "title": "Inventory",
      "type": "object",
      "additionalProperties": {
        "type": "integer"
      }
    }
  },
  "required": [
    "slug",
    "inventory"
  ]
}